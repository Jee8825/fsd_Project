option_settings:
  # On Amazon Linux 2023, the start command comes from Procfile.
  # We only set env defaults + the proxy server here.
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
    PORT: 8080
  aws:elasticbeanstalk:environment:proxy:
    ProxyServer: nginx
